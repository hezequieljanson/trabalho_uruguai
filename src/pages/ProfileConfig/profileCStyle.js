import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {},
  topHalf: {
    width: '100%',
    height: 400,
    backgroundColor: 'blue',
  },
  editPhotoIcon: {
    position: 'absolute',
  },
});
